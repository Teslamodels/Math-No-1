# The question 👇
# x and y given a real number. Compute: max(x, y) and min(x, y).

# The answer 👇

x, y = map(float, input("This is me!\n").split())

a = max(x, y)

b = min(x, y)

print(a, b)    
